# Copie este arquivo para .env (local) ou use Environment Variables no Render

# Token do Bot Discord (OBRIGATÓRIO)
DISCORD_TOKEN=seu_token_discord_aqui

# Nota: Seu bot usa:
# - DATABASE: bot/bot_zeus.db (SQLite)
# - PREFIXO: ! (padrão)
# - Owner: emanoel7269 (hardcoded)

# ⚠️ IMPORTANTE: No Render, não coloque .env no repositório!
# Use: Render → Environment Variables → Add Variable
# Adicione: DISCORD_TOKEN = seu_token
